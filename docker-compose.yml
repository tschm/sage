version: '3.5'
services:
  web:
     build:
       context: .
       dockerfile: Dockerfile
     volumes:
       - ./books:/opt/app/data
     ports:
        - "8885:8888"
     command:
       sage-jupyter
